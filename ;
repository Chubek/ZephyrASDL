




SumType <- Constructor ( '|' Constructor ) / Attributes

Attributes <- "attributes" Fields

Constructor <- ConIdent Fields?

Fields <- '(' Field ( ',' Field ) ')'
Field <- TypeIdent

ConIdent <- [A-Z][a-zA-Z_0-9]*
TypeIdent <- [a-z][A-Za-z0-9_]*

_ <- [ \r\n\t]+

EOF <- !.
